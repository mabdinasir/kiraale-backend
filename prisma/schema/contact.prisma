model Contact {
    id            String      @id @default(uuid())
    firstName      String
    lastName      String
    subject       String
    message       String
    email         String     
    createdAt     DateTime    @default(now())
}